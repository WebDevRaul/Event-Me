import styled from 'styled-components';

const StyledRecentEvents = styled.section`
  padding: 20px;
  form {
    .submit {
      margin: 0 5%;
      display: flex;
      justify-content: space-between;
    }
  }
`

export default StyledRecentEvents;